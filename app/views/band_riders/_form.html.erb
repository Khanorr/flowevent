<%= form_with(model: band_rider) do |form| %>
  <% if band_rider.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(band_rider.errors.count, "error") %> prohibited this band_rider from being saved:</h2>

      <ul>
        <% band_rider.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :band_id, style: "display: block" %>
    <%= form.collection_select :band_id, Band.all, :id, :name, prompt: true %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :version, style: "display: block" %>
    <%= form.number_field :version %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
